<h2>Catalogue des commandes</h2>
<?php 
foreach($this->messages as $message)
	echo $message;
?>
<div id="texteDroite2">
	<?php if($this->livre){?>
	<a href="<?= $this->baseUrl('liste_commandes/livre');?>">Afficher toutes les
		commandes</a>
	<?php } else {?>
	<a href="<?= $this->baseUrl('liste_commandes');?>">Afficher uniquement
		les commandes non livrés</a>
	<?php } ?>
</div>
<?php if($this->paginator->count() > 0) {?>

<table class="liste">
	<thead>
		<tr class="Head">
			<?php 
			echo $this->HeadId;
			echo $this->HeadNom;
			echo $this->HeadPrenom;
			echo $this->HeadNombre;
			echo $this->HeadMontant;
			echo $this->HeadDate;
			echo $this->HeadLivre;
			?>
			<th></th>
		</tr>
	</thead>
	<tbody>
		<?php
		foreach ($this->paginator as $commande)
					{?>
		<tr class="<?= $this->cycle(array("Odd", "Even"))->next();?>">
			<td><a
				href="<?= $this->baseUrl('fiche_commande/').$commande->id_commande; ?>"
				class="idLigneCommande"><?= $commande->id_commande; ?> </a>
			</td>
			<td><a
				href="<?= $this->baseUrl('fiche_commande/').$commande->id_commande; ?>"
				class="nomLigneCommande"><?= ($commande->nom) ? $commande->nom : "Inconnu"; ?>
			</a>
			</td>
			<td><a
				href="<?= $this->baseUrl('fiche_commande/').$commande->id_commande; ?>"
				class="prenomLigneCommande"><?= ($commande->prenom) ? $commande->prenom : "Iconnu"; ?>
			</a>
			</td>
			<td><a
				href="<?= $this->baseUrl('fiche_commande/').$commande->id_commande; ?>"
				class="nombreLigneCommande"><?= $commande->num; ?> </a>
			</td>
			<td><a
				href="<?= $this->baseUrl('fiche_commande/').$commande->id_commande; ?>"
				class="montantLigneCommande"><?= $commande->montant."€"; ?> </a>
			</td>
			<td><a
				href="<?= $this->baseUrl('fiche_commande/').$commande->id_commande; ?>"
				class="dateLigneCommande"><?= date("d/m/y à H:i",strtotime($commande->date));?>
			</a>
			</td>
			<td><a
				href="<?= $this->baseUrl('fiche_commande/').$commande->id_commande; ?>"
				class="montantLigneCommande"> <?php if($commande->Islivre){?> <span
					id='LivreOk'>Livrée</span> <?php }else { ?> <span id='LivreNok'>Non
						livrée</span> <?php }?>
			</a>
			</td>
			<td><a href="<?= $this->baseUrl('livre_commande/'.$commande->id_commande.'/'); ?><?= ($commande->Islivre) ? 'nonlivre' : 'livre'; ?>" class="boutonConfigCommande" title="<?= ($commande->Islivre) ? 'Marqué comme non livré' : 'Marqué comme livré'; ?>"></a>
		</tr>
		<?php }?>
	</tbody>
</table>
<?= $this->pagination; ?>
<?php } else {?>
<div class="CommandeLivre">Toutes les commandes sont livrées</div>
<?php } ?>